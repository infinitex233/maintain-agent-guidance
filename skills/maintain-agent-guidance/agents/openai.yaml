interface:
  display_name: "Maintain Agent Guidance"
  short_description: "Keep repository agent guidance current"
  default_prompt: "Use $maintain-agent-guidance to enable completion-time guidance maintenance for this repository."
policy:
  allow_implicit_invocation: true
